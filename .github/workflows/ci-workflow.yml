name: Workflow CI

on:
  push:
    branches: [ "actions" ]
    paths: 
  pull_request:
    branches: [ "actions" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      # we need the docker built and running so it is live for the actions tests to run
    - name: Build the Docker image
      run: docker build -t liatrio_exercise:latest .

    - name: Run app to test
      run: |
        docker run -d -p 80:80 liatrio_exercise:latest
        sleep 7

    - name: Run tests
      uses: liatrio/github-actions/apprentice-action@v1.0.0

    - name: Log in to Docker Hub
      uses: docker/login-action@v3.0.0
      with:
        # Hide your secret information
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Tag image for Docker Hub
      run: docker tag liatrio_exercise:latest docker.io/asvirdi1/liatrio_exercise:latest

    - name: Push image to Docker Hub
      run: docker push docker.io/asvirdi1/liatrio_exercise:latest

    

